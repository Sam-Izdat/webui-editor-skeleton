import type DocumentSession from '$lib/doc_types';

// TODO: ...

export const save = (session: DocumentSession) => null;

export const load = (uuid: string) => null;

export const remove = (uuid: string) => null;

export const rename = (uuid: string, newName: string) => null;

export const search = (substring: string) => null;

export const list = (descending = true) => null;